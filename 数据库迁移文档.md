# 数据库迁移文档

## 目标

- [x] 将 `apps/control-plane-api` 从 SQLite 全量迁移到 MySQL 8.0
- [x] 保持 `apps/proxy-node` 继续使用 SQLite，不做数据库替换
- [x] 为 `control-plane-api` 引入现代化、易管理的数据库框架

## 迁移顺序

### 1. 现状梳理

- [x] 确认 `control-plane-api` 当前数据库入口、配置项、建表方式、store 实现位置
- [x] 梳理所有 SQLite 绑定点
- [x] 梳理运行脚本、健康检查、文档中的 SQLite 暴露点

### 2. 数据库框架接入

- [x] 选定 MySQL 访问框架
- [x] 在 `go.mod` 中引入 MySQL 驱动和数据库框架依赖
- [x] 移除 `control-plane-api` 对 SQLite 驱动的依赖

### 3. 配置与启动链路迁移

- [x] 将 `SQLITE_PATH` 配置替换为 MySQL 配置
- [x] 更新 `internal/config` 的配置结构
- [x] 更新 `internal/app` 的 store 初始化逻辑
- [x] 更新 `internal/httpapi` 中对数据库后端的健康信息暴露

### 4. Schema 与迁移机制

- [x] 为 MySQL 8.0 定义 schema / migration 入口
- [x] 处理主键、唯一索引、外键、布尔值、文本字段的 MySQL 类型映射
- [x] 处理初始化建库 / 建表逻辑
- [x] 处理 bootstrap admin 和默认 topology 初始化逻辑

### 5. Store 实现迁移

- [x] 新建 MySQL store
- [x] 迁移账户与会话相关方法
- [x] 迁移节点、链路、拓扑相关方法
- [x] 迁移 onboarding / access path / bootstrap token 相关方法
- [x] 迁移 route rule / chain / policy revision 相关方法
- [x] 迁移 node-agent token / heartbeat / certificate 相关方法
- [x] 迁移清理任务和状态刷新任务
- [x] 删除或停用 `control-plane-api` 的 SQLite store 实现

### 6. 兼容性与运行脚本

- [x] 更新后端启动脚本中的数据库依赖说明
- [x] 确认不影响 `proxy-node` 的 SQLite 使用
- [x] 确认前端代理层不需要数据库类型改动

### 7. 验证

- [x] 编译 `apps/control-plane-api`
- [x] 验证启动流程可连接 MySQL
- [x] 验证登录、节点 CRUD、onboarding、route、policy、heartbeat 主链路
- [x] 验证 scheduler 相关逻辑

### 8. 文档收尾

- [x] 更新后端技术文档中的数据库说明
- [x] 更新运行说明中的环境变量
- [x] 回填本迁移文档的完成状态
